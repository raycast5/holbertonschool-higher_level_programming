#!/usr/bin/node
// Creates a class Rectangle that takes w and h

module.exports = class Rectangle {
  constructor (w, h) {
    if (w > 0 && h > 0) {
      this.width = w;
      this.height = h;
    }
  }
};
